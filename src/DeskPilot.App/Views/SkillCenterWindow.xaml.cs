using System.Windows;

namespace DeskPilot.App.Views;

/// <summary>v0.15: 技能中心窗口（独立 Window，Melon 风格）。承载市场 / 已安装 / 更新 3 个 Tab。</summary>
public partial class SkillCenterWindow : Window
{
    public SkillCenterWindow()
    {
        InitializeComponent();
    }
}